{% extends 'base.html.twig' %}

{% block title %}JobSector{% endblock %}

{% block body %}
    <h1>JobSector</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_sector.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ job_sector.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('htcintranet_job_sector_index') }}">back to list</a>

    <a href="{{ path('htcintranet_job_sector_edit', {'id': job_sector.id}) }}">edit</a>

    {{ include('job_sector/_delete_form.html.twig') }}
{% endblock %}
